var message, success, error;

if (<%= @method == 'buy' %>) {
  $('.message')
    .show()
    .html('<%= @errors.length > 0 ? @errors.join('<br /><br />').html_safe : @success %>'); 

  <% if @success %>
    $('.message').removeClass('message-error');
    $('.message').addClass('message-success');
    $('.user-cash').text('<%= number_to_currency(current_user.cash, precision: 2) %>');
    $('.table-container').html('<%= j render 'portfolio/table' %>');
    $('.table').DataTable().draw(false);
  <% else %>
    $('.message').removeClass('message-success');
    $('.message').addClass('message-error');
  <% end %>
} else {
  $('.sell-message')
    .show()
    .html('<%= @errors.length > 0 ? @errors.join('<br /><br />').html_safe : @success %>'); 

  <% if @success %>
    $('.sell-message').removeClass('sell-message-error');
    $('.sell-message').addClass('sell-message-success');
    $('.user-cash').text('<%= number_to_currency(current_user.cash, precision: 2) %>');
    $('.table-container').html('<%= j render 'portfolio/table' %>');
    $('.table').DataTable().draw(false);
  <% else %>
    $('.sell-message').removeClass('sell-message-success');
    $('.sell-message').addClass('sell-message-error');
  <% end %>
}
